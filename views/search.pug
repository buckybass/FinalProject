extends layout/main
block content
  .container.mt-3
    include components/alert
    h1 ค้นหาฐานข้อมูลปริญญานิพนธ์
    .card.mt-3
      .card-body
        form
          input.form-control(type="text", name="",placeholder="ค้นหาเลย")
          .row.mt-3
            .col-md-6
              select#floatingSelect.form-select(name='category')
                option(selected='') กรุณาเลือกหมวดหมู่ปริญญานิพนธ์
                each category in categorys
                  option(value=category.name)=category.name
            .col-md-6
              select.form-select(name='teacher')
                option(selected='') กรุณาเลือกอาจารย์ที่ปรึกษา
                each teacher in teachers
                  option(value=teacher.firstname)=teacher.firstname + ' ' + teacher.lastname
          input.btn.btn-success.mt-2(type="button", value="ค้นหา")
    .card.mt-3
      .card-body
        h1.text-center ข้อมูลปริญญานิพนธ์ทั้งหมด
        table.table
          thead
            tr
              th(scope='col')
              th(scope='col') ชื่อปริญญานิพนธ์
              th(scope='col') ผู้จัดทำ
              th(scope='col') สาขา
              th(scope='col') คณะ
          tbody
            -let i=0
            each books in book
              tr
                th(scope='row')=i=i+1
                td
                  a(href=`/book/detail/${books._id}`) #{books.bookname}
                td=books.firstname+' '+books.lastname
                td=books.branch
                td=books.faculty

