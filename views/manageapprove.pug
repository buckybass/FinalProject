extends layout/main
block content
  .container
    .card.mt-3
      .card-body
        include components/alert
        h1.text-center ข้อมูลการอนุมัติปริญญานิพนธ์ทั้งหมด
        h3.text-center อนุมัติแล้ว
        table.table
          thead
            tr
              th(scope='col')
              th(scope='col') ชื่อปริญญานิพนธ์
              th(scope='col') ผู้จัดทำ
              th(scope='col') สาขา
              th(scope='col') คณะ
              th(scope='col') อาจารย์ที่ปรึกษา
              th(scope='col') สถานะ
          tbody
            -let i=0
            each book in books
              tr
                th(scope='row')=i=i+1
                  td
                    a(href=`/book/detail/${book._id}`) #{book.bookname}
                  td=book.firstname+' '+book.lastname
                  td=book.branch
                  td=book.faculty
                  td=book.teacher
                  td
                    if book.approve
                      .text-success อนุมัติแล้ว
                    else
                      .text-warning รออนุมัติ

